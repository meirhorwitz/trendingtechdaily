rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Only admins can upload images, anyone can view them
    match /article-images/{imageId} {
      allow read: if true;
      allow write: if request.auth != null && request.auth.token.admin == true;
    }
  }
}